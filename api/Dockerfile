# Dockerfile

# Шаг 1: Выбираем базовый образ
# Мы используем официальный образ Python 3.9 в "slim" версии.
# Он лёгкий и содержит всё необходимое для запуска нашего приложения.
FROM python:3.9-slim

# Шаг 2: Устанавливаем рабочую директорию внутри контейнера
# Все последующие команды будут выполняться в этой папке.
WORKDIR /app

# Шаг 3: Копируем файл с зависимостями и устанавливаем их
# Копируем сначала только этот файл, чтобы Docker мог использовать кэш.
# Если мы не меняем зависимости, Docker не будет переустанавливать их при каждой сборке.
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Шаг 4: Копируем весь остальной код нашего приложения
COPY . .

# Шаг 5: Указываем команду для запуска приложения
# Эта команда запустит наш FastAPI-сервис с помощью uvicorn
# сделав его доступным внутри сети Docker на порту 8000.
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]